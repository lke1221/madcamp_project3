
import React from 'react';

const NewNotice = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'column',
        paddingTop: 80,
      }}
    >
        <div style={{width: 900}}>
            <h1>New Notice</h1>
        </div>
    </div>
  );
};

export default NewNotice;